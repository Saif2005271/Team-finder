# Team-Finder Backend — Python Dependencies
# Install with: pip install -r requirements.txt

fastapi==0.111.0          # Web framework for building the REST API
uvicorn[standard]==0.29.0  # ASGI server to run FastAPI
pydantic[email]==2.7.1    # Data validation and settings management
pyjwt==2.8.0              # JSON Web Token creation and verification
anthropic==0.25.6         # Anthropic Claude API client (for AI assistant)
httpx==0.27.0             # HTTP client (used by Anthropic SDK)
python-dotenv==1.0.1      # Load environment variables from a .env file

# Optional — install for production password hashing:
# bcrypt==4.1.3

# Optional — install for production PostgreSQL/Supabase:
# supabase==2.4.3
# psycopg2-binary==2.9.9
