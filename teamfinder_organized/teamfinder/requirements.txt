# TeamFinder — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework + server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Validation / settings
pydantic==2.7.1
python-dotenv==1.0.1

# JWT
pyjwt==2.8.0

# Password hashing
bcrypt==4.1.3

# Database (SQLAlchemy + MySQL driver; SQLite is built into Python)
sqlalchemy==2.0.30
pymysql==1.1.1
cryptography==42.0.8

# External advisor service (optional — rule-based fallback used if absent)
anthropic==0.25.6
